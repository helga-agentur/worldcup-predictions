services:
  predictions:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      PYTHONPATH: /app/src
      BASE_URL: ${BASE_URL:-}
    command: worldcup-predictions --help
